# LiDAR Object Detection with ICP Free Space {Ready for deployment}

This is the older version of object and free space detection without interprocess communication and handles visualization and inferences within the same application.


This application requires Eigen3 to be installed before building.

Install Eigen3 using:
```bash
sudo apt-get install libeigen3-dev
```
